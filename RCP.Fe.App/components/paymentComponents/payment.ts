export type MovieInfo = {
  title: string;
  subtitle: string;
};

export type BookingInfo = {
  cinema: string;
  date: string;
  time: string;
  room: string;
  ticketType: string;
};

export type Combo = {
  id: string;
  name: string;
  description: string;
  price: number;
  image: string;
};
